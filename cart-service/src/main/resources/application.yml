server:
  port: 8084

spring:
  application:
    name: cart-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/ecommerce_cart
      database: ecommerce_cart

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
